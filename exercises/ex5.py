# create a function that takes in two parameters
def replace_period(sentence, punctuation):
    # use replace method to replace the period with !.
    period = sentence.replace(".", punctuation)
    # Return: Gives back a value once the funnction is done with the functionality.
    return period





# sentence = "Test.  This is a test.  Testing."
# sentence2 = replace_period(sentence, "!")
# print(sentence2)
